# FLAT Homepage

Temporary static deployment package for the FLAT social cooperative homepage.

## Deploy

This directory can be deployed as a static site through GitHub Pages, Netlify, Vercel, or any standard static hosting service.

For GitHub Pages, push this directory to the root of a repository and enable Pages from GitHub Actions or the `main` branch.
